name: BALI_COCKTAIL_ADMIN
options:
  bundleIdPrefix: by.bali.cocktails.admin
targets:
  BALI_COCKTAIL_ADMIN:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - BALI_COCKTAIL_ADMIN
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: by.bali.cocktails.admin.mobile
        PRODUCT_NAME: BALI COCKTAIL ADMIN
        SWIFT_VERSION: 5.0
        INFOPLIST_KEY_CFBundleDisplayName: BALI ADMIN
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    scheme:
      testTargets: []
